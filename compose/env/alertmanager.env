# alertmanager — no secrets.
#
# Receiver URLs that carry a token in the path (a Slack webhook, a PagerDuty
# routing key) are read from files by `*_url_file` / `*_key_file` in
# ../../alertmanager/alertmanager.yml, never from here and never from the
# committed config.
TZ=UTC
