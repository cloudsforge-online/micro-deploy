# GENERATED by scripts/k8s-render.py — do not hand-edit.
#
# Network:   testnet
# Namespace: cf-testnet
# Release:   2026.8.99  (2026.8.99.yaml)
# Source:    compose/docker-compose.estate.yml + render-vars.testnet.yaml
#
# Regenerate with:
#   ./scripts/k8s-render.py --network testnet --release ../org/releases/2026.8.99.yaml \
#       --outdir k8s/estate/testnet
#
# Contains NO secret values. Every credential is a secretKeyRef by name; see the
# script's docstring for the three shapes and why each is handled the way it is.
---
apiVersion: v1
kind: Service
metadata:
  name: activity
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: activity
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: activity.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: admin-api
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: admin-api
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: admin-api.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: agora
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: agora
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: agora.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: beacon
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: beacon
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: beacon.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: billing
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: billing
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: billing.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: community
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: community
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: community.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: custody
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: custody
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: custody.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: devplatform
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: devplatform
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: devplatform.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: emberkin
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: emberkin
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: emberkin.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: faucet
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: faucet
    app.kubernetes.io/part-of: cloudsforge-estate
    online.cloudsforge.network: testnet
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: faucet
  ports:
  - name: p4000
    port: 4000
    targetPort: 4000
---
apiVersion: v1
kind: Service
metadata:
  name: foresight
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: foresight
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: foresight.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: hub-api
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: hub-api
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: hub-api.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: identity
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: identity
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: identity.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: indexer
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: indexer
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: indexer.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: lantern
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: lantern
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: lantern.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: ledger
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: ledger
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: ledger.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: market
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: market
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: market.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: mint
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: mint
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: mint.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: nda
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: nda
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: nda.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: policy
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: policy
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: policy.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: pricing
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: pricing
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: pricing.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: settlement
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: settlement
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: settlement.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: site
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: site
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: site.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: studio
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: studio
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: studio.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: tessera
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: tessera
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: tessera.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: trade
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: trade
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: trade.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: wallet
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: wallet
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: wallet.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: worlds
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: worlds
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Consolidated: one pod in cloudsforge-estate serves both estates. A CNAME rather than a deletion so the gateway''s namespace-local backend and every service-to-service call keep resolving unchanged.'
spec:
  type: ExternalName
  externalName: worlds.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: aetherholm
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: aetherholm
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: "Merged into emberkin: this service's code now runs inside that pod as a module, so it has no Deployment and no migrate Job of its own \u2014 a second migrator against a schema that pod now owns is the fastest way to corrupt it. A CNAME rather than a deletion so service-to-service calls keep resolving unchanged."
spec:
  type: ExternalName
  externalName: emberkin.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: analytics
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: analytics
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: "Merged into lantern: this service's code now runs inside that pod as a module, so it has no Deployment and no migrate Job of its own \u2014 a second migrator against a schema that pod now owns is the fastest way to corrupt it. A CNAME rather than a deletion so service-to-service calls keep resolving unchanged."
spec:
  type: ExternalName
  externalName: lantern.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: notify
  namespace: cf-testnet
  labels:
    app.kubernetes.io/name: notify
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: "Merged into activity: this service's code now runs inside that pod as a module, so it has no Deployment and no migrate Job of its own \u2014 a second migrator against a schema that pod now owns is the fastest way to corrupt it. A CNAME rather than a deletion so service-to-service calls keep resolving unchanged."
spec:
  type: ExternalName
  externalName: activity.cloudsforge-estate.svc.cluster.local
