# GENERATED by scripts/k8s-render.py — do not hand-edit.
#
# Network:   testnet
# Namespace: cf-testnet
# Release:   2026.8.104  (2026.8.104.yaml)
# Source:    compose/docker-compose.estate.yml + render-vars.testnet.yaml
#
# Regenerate with:
#   ./scripts/k8s-render.py --network testnet --release ../org/releases/2026.8.104.yaml \
#       --outdir k8s/estate/testnet
#
# Contains NO secret values. Every credential is a secretKeyRef by name; see the
# script's docstring for the three shapes and why each is handled the way it is.
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: custody-keys
  namespace: cf-testnet
  labels:
    online.cloudsforge.network: testnet
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: cf-retain
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: studio-assets
  namespace: cf-testnet
  labels:
    online.cloudsforge.network: testnet
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: cf-retain
  resources:
    requests:
      storage: 10Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: world-assets
  namespace: cf-testnet
  labels:
    online.cloudsforge.network: testnet
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: cf-retain
  resources:
    requests:
      storage: 1Gi
