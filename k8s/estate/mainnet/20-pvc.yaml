# GENERATED by scripts/k8s-render.py — do not hand-edit.
#
# Network:   mainnet
# Namespace: cloudsforge-estate
# Release:   2026.8.86  (2026.8.86.yaml)
# Source:    compose/docker-compose.estate.yml + render-vars.mainnet.yaml
#
# Regenerate with:
#   ./scripts/k8s-render.py --network mainnet --release ../org/releases/2026.8.86.yaml \
#       --outdir k8s/estate/mainnet
#
# Contains NO secret values. Every credential is a secretKeyRef by name; see the
# script's docstring for the three shapes and why each is handled the way it is.
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: custody-keys
  namespace: cloudsforge-estate
  labels:
    online.cloudsforge.network: mainnet
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: cf-retain
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: studio-assets
  namespace: cloudsforge-estate
  labels:
    online.cloudsforge.network: mainnet
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: cf-retain
  resources:
    requests:
      storage: 10Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: world-assets
  namespace: cloudsforge-estate
  labels:
    online.cloudsforge.network: mainnet
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: cf-retain
  resources:
    requests:
      storage: 1Gi
