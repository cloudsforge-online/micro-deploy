# GENERATED by scripts/k8s-render.py — do not hand-edit.
#
# Network:   mainnet
# Namespace: cloudsforge-estate
# Release:   2026.8.108  (2026.8.108.yaml)
# Source:    compose/docker-compose.estate.yml + render-vars.mainnet.yaml
#
# Regenerate with:
#   ./scripts/k8s-render.py --network mainnet --release ../org/releases/2026.8.108.yaml \
#       --outdir k8s/estate/mainnet
#
# Contains NO secret values. Every credential is a secretKeyRef by name; see the
# script's docstring for the three shapes and why each is handled the way it is.
---
apiVersion: v1
kind: Service
metadata:
  name: agora
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: agora
    app.kubernetes.io/part-of: cloudsforge-estate
    online.cloudsforge.network: mainnet
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: agora
  ports:
  - name: p4000
    port: 4000
    targetPort: 4000
---
apiVersion: v1
kind: Service
metadata:
  name: beacon
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: beacon
    app.kubernetes.io/part-of: cloudsforge-estate
    online.cloudsforge.network: mainnet
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: beacon
  ports:
  - name: p4000
    port: 4000
    targetPort: 4000
---
apiVersion: v1
kind: Service
metadata:
  name: custody
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: custody
    app.kubernetes.io/part-of: cloudsforge-estate
    online.cloudsforge.network: mainnet
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: custody
  ports:
  - name: p4000
    port: 4000
    targetPort: 4000
---
apiVersion: v1
kind: Service
metadata:
  name: identity
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: identity
    app.kubernetes.io/part-of: cloudsforge-estate
    online.cloudsforge.network: mainnet
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: identity
  ports:
  - name: p4000
    port: 4000
    targetPort: 4000
---
apiVersion: v1
kind: Service
metadata:
  name: indexer
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: indexer
    app.kubernetes.io/part-of: cloudsforge-estate
    online.cloudsforge.network: mainnet
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: indexer
  ports:
  - name: p4000
    port: 4000
    targetPort: 4000
---
apiVersion: v1
kind: Service
metadata:
  name: ledger
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: ledger
    app.kubernetes.io/part-of: cloudsforge-estate
    online.cloudsforge.network: mainnet
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ledger
  ports:
  - name: p4000
    port: 4000
    targetPort: 4000
---
apiVersion: v1
kind: Service
metadata:
  name: pool
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: pool
    app.kubernetes.io/part-of: cloudsforge-estate
    online.cloudsforge.network: mainnet
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pool
  ports:
  - name: p4000
    port: 4000
    targetPort: 4000
  - name: p3334
    port: 3334
    targetPort: 3334
  - name: p3333
    port: 3333
    targetPort: 3333
---
apiVersion: v1
kind: Service
metadata:
  name: pool-stratum
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: pool
    app.kubernetes.io/part-of: cloudsforge-estate
    online.cloudsforge.network: mainnet
  annotations:
    online.cloudsforge.why: 'Bound to 0.0.0.0 under compose because miners connect from the internet. The only service in the estate not fronted by Traefik: Stratum is a persistent line-delimited JSON socket, not HTTP.'
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: pool
  ports:
  - name: stratum-3333
    port: 3333
    targetPort: 3333
  - name: stratum-3334
    port: 3334
    targetPort: 3334
---
apiVersion: v1
kind: Service
metadata:
  name: settlement
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: settlement
    app.kubernetes.io/part-of: cloudsforge-estate
    online.cloudsforge.network: mainnet
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: settlement
  ports:
  - name: p4000
    port: 4000
    targetPort: 4000
---
apiVersion: v1
kind: Service
metadata:
  name: status-web
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: status-web
    app.kubernetes.io/part-of: cloudsforge-estate
    online.cloudsforge.network: mainnet
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: status-web
  ports:
  - name: p8080
    port: 8080
    targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: activity
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: activity
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: "Merged into agora: this service's code now runs inside that pod as a module, so it has no Deployment and no migrate Job of its own \u2014 a second migrator against a schema that pod now owns is the fastest way to corrupt it. A CNAME rather than a deletion so service-to-service calls keep resolving unchanged."
spec:
  type: ExternalName
  externalName: agora.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: admin-api
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: admin-api
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: "Merged into agora: this service's code now runs inside that pod as a module, so it has no Deployment and no migrate Job of its own \u2014 a second migrator against a schema that pod now owns is the fastest way to corrupt it. A CNAME rather than a deletion so service-to-service calls keep resolving unchanged."
spec:
  type: ExternalName
  externalName: agora.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: aetherholm
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: aetherholm
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: "Merged into agora: this service's code now runs inside that pod as a module, so it has no Deployment and no migrate Job of its own \u2014 a second migrator against a schema that pod now owns is the fastest way to corrupt it. A CNAME rather than a deletion so service-to-service calls keep resolving unchanged."
spec:
  type: ExternalName
  externalName: agora.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: analytics
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: analytics
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: "Merged into agora: this service's code now runs inside that pod as a module, so it has no Deployment and no migrate Job of its own \u2014 a second migrator against a schema that pod now owns is the fastest way to corrupt it. A CNAME rather than a deletion so service-to-service calls keep resolving unchanged."
spec:
  type: ExternalName
  externalName: agora.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: billing
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: billing
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: "Merged into agora: this service's code now runs inside that pod as a module, so it has no Deployment and no migrate Job of its own \u2014 a second migrator against a schema that pod now owns is the fastest way to corrupt it. A CNAME rather than a deletion so service-to-service calls keep resolving unchanged."
spec:
  type: ExternalName
  externalName: agora.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: community
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: community
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: "Merged into agora: this service's code now runs inside that pod as a module, so it has no Deployment and no migrate Job of its own \u2014 a second migrator against a schema that pod now owns is the fastest way to corrupt it. A CNAME rather than a deletion so service-to-service calls keep resolving unchanged."
spec:
  type: ExternalName
  externalName: agora.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: devplatform
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: devplatform
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: "Merged into agora: this service's code now runs inside that pod as a module, so it has no Deployment and no migrate Job of its own \u2014 a second migrator against a schema that pod now owns is the fastest way to corrupt it. A CNAME rather than a deletion so service-to-service calls keep resolving unchanged."
spec:
  type: ExternalName
  externalName: agora.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: emberkin
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: emberkin
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: "Merged into agora: this service's code now runs inside that pod as a module, so it has no Deployment and no migrate Job of its own \u2014 a second migrator against a schema that pod now owns is the fastest way to corrupt it. A CNAME rather than a deletion so service-to-service calls keep resolving unchanged."
spec:
  type: ExternalName
  externalName: agora.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: foresight
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: foresight
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: "Merged into agora: this service's code now runs inside that pod as a module, so it has no Deployment and no migrate Job of its own \u2014 a second migrator against a schema that pod now owns is the fastest way to corrupt it. A CNAME rather than a deletion so service-to-service calls keep resolving unchanged."
spec:
  type: ExternalName
  externalName: agora.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: hub-api
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: hub-api
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: "Merged into agora: this service's code now runs inside that pod as a module, so it has no Deployment and no migrate Job of its own \u2014 a second migrator against a schema that pod now owns is the fastest way to corrupt it. A CNAME rather than a deletion so service-to-service calls keep resolving unchanged."
spec:
  type: ExternalName
  externalName: agora.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: lantern
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: lantern
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: "Merged into agora: this service's code now runs inside that pod as a module, so it has no Deployment and no migrate Job of its own \u2014 a second migrator against a schema that pod now owns is the fastest way to corrupt it. A CNAME rather than a deletion so service-to-service calls keep resolving unchanged."
spec:
  type: ExternalName
  externalName: agora.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: market
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: market
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: "Merged into agora: this service's code now runs inside that pod as a module, so it has no Deployment and no migrate Job of its own \u2014 a second migrator against a schema that pod now owns is the fastest way to corrupt it. A CNAME rather than a deletion so service-to-service calls keep resolving unchanged."
spec:
  type: ExternalName
  externalName: agora.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: mint
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: mint
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: "Merged into agora: this service's code now runs inside that pod as a module, so it has no Deployment and no migrate Job of its own \u2014 a second migrator against a schema that pod now owns is the fastest way to corrupt it. A CNAME rather than a deletion so service-to-service calls keep resolving unchanged."
spec:
  type: ExternalName
  externalName: agora.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: nda
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: nda
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: "Merged into agora: this service's code now runs inside that pod as a module, so it has no Deployment and no migrate Job of its own \u2014 a second migrator against a schema that pod now owns is the fastest way to corrupt it. A CNAME rather than a deletion so service-to-service calls keep resolving unchanged."
spec:
  type: ExternalName
  externalName: agora.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: notify
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: notify
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: "Merged into agora: this service's code now runs inside that pod as a module, so it has no Deployment and no migrate Job of its own \u2014 a second migrator against a schema that pod now owns is the fastest way to corrupt it. A CNAME rather than a deletion so service-to-service calls keep resolving unchanged."
spec:
  type: ExternalName
  externalName: agora.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: policy
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: policy
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: "Merged into agora: this service's code now runs inside that pod as a module, so it has no Deployment and no migrate Job of its own \u2014 a second migrator against a schema that pod now owns is the fastest way to corrupt it. A CNAME rather than a deletion so service-to-service calls keep resolving unchanged."
spec:
  type: ExternalName
  externalName: agora.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: pricing
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: pricing
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: "Merged into agora: this service's code now runs inside that pod as a module, so it has no Deployment and no migrate Job of its own \u2014 a second migrator against a schema that pod now owns is the fastest way to corrupt it. A CNAME rather than a deletion so service-to-service calls keep resolving unchanged."
spec:
  type: ExternalName
  externalName: agora.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: studio
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: studio
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: "Merged into agora: this service's code now runs inside that pod as a module, so it has no Deployment and no migrate Job of its own \u2014 a second migrator against a schema that pod now owns is the fastest way to corrupt it. A CNAME rather than a deletion so service-to-service calls keep resolving unchanged."
spec:
  type: ExternalName
  externalName: agora.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: tessera
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: tessera
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: "Merged into agora: this service's code now runs inside that pod as a module, so it has no Deployment and no migrate Job of its own \u2014 a second migrator against a schema that pod now owns is the fastest way to corrupt it. A CNAME rather than a deletion so service-to-service calls keep resolving unchanged."
spec:
  type: ExternalName
  externalName: agora.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: trade
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: trade
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: "Merged into agora: this service's code now runs inside that pod as a module, so it has no Deployment and no migrate Job of its own \u2014 a second migrator against a schema that pod now owns is the fastest way to corrupt it. A CNAME rather than a deletion so service-to-service calls keep resolving unchanged."
spec:
  type: ExternalName
  externalName: agora.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: wallet
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: wallet
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: "Merged into agora: this service's code now runs inside that pod as a module, so it has no Deployment and no migrate Job of its own \u2014 a second migrator against a schema that pod now owns is the fastest way to corrupt it. A CNAME rather than a deletion so service-to-service calls keep resolving unchanged."
spec:
  type: ExternalName
  externalName: agora.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: worlds
  namespace: cloudsforge-estate
  labels:
    app.kubernetes.io/name: worlds
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: "Merged into agora: this service's code now runs inside that pod as a module, so it has no Deployment and no migrate Job of its own \u2014 a second migrator against a schema that pod now owns is the fastest way to corrupt it. A CNAME rather than a deletion so service-to-service calls keep resolving unchanged."
spec:
  type: ExternalName
  externalName: agora.cloudsforge-estate.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: faucet
  labels:
    app.kubernetes.io/name: faucet
    app.kubernetes.io/part-of: cloudsforge
  annotations:
    online.cloudsforge.why: 'Testnet-only service, reached from here because the TESTNET gateway now runs in this namespace. The mainnet gateway emits no router and no upstream for it: both sit inside {{ if eq (env "CF_EMBER_NETWORK") "testnet" }}, so this name is resolvable only from the testnet router table.'
spec:
  type: ExternalName
  externalName: faucet.cf-testnet.svc.cluster.local
  ports:
  - name: p4000
    port: 4000
    targetPort: 4000
